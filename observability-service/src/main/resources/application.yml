server:
  port: 8085

spring:
  application:
    name: observability-service

management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus
  endpoint:
    health:
      show-details: always
